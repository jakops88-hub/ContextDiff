# ContextDiff API - Environment Configuration Template
# Copy this file to .env and fill in your actual values

# ============================================================================
# REQUIRED: OpenAI Configuration
# ============================================================================
# Your OpenAI API key (get it from https://platform.openai.com/api-keys)
OPENAI_API_KEY=your_openai_api_key_here

# ============================================================================
# OPTIONAL: API Security
# ============================================================================
# Secret key for RapidAPI authentication (leave empty for local development)
# Generate a secure random string: openssl rand -hex 32
# API_SECRET=your_secure_api_secret_here

# ============================================================================
# OPTIONAL: OpenAI Model Settings
# ============================================================================
# Model to use for semantic analysis
# Options: gpt-4o, gpt-4o-mini, gpt-4-turbo, gpt-3.5-turbo
# Default: gpt-4o-mini (best balance of cost and performance)
OPENAI_MODEL=gpt-4o-mini

# Temperature for LLM responses (0.0 = deterministic, 1.0 = creative)
# Default: 0.1 (low for consistency in analysis)
OPENAI_TEMPERATURE=0.1

# Maximum tokens for LLM response
# Default: 4000 (sufficient for detailed analysis)
OPENAI_MAX_TOKENS=4000

# API timeout in seconds
# Default: 60
OPENAI_TIMEOUT=60

# ============================================================================
# OPTIONAL: API Configuration
# ============================================================================
# API title displayed in documentation
API_TITLE=ContextDiff API

# API version
API_VERSION=1.0.0

# API description
API_DESCRIPTION=Semantic text difference analysis using AI

# ============================================================================
# OPTIONAL: CORS Configuration
# ============================================================================
# Comma-separated list of allowed origins (use "*" for all, not recommended in production)
# Example: ALLOW_ORIGINS=https://example.com,https://app.example.com
# Default: * (allow all - change for production)
ALLOW_ORIGINS=*

# ============================================================================
# OPTIONAL: Rate Limiting
# ============================================================================
# Maximum requests per minute per IP address
# Default: 60
RATE_LIMIT_PER_MINUTE=60

# Additional burst capacity above rate limit
# Default: 10
RATE_LIMIT_BURST=10

# ============================================================================
# OPTIONAL: Monitoring (Sentry)
# ============================================================================
# Sentry DSN for error tracking (leave empty to disable)
# Get from: https://sentry.io/settings/YOUR_ORG/projects/YOUR_PROJECT/keys/
SENTRY_DSN=

# Environment name for Sentry (production, staging, development)
SENTRY_ENVIRONMENT=production

# Percentage of transactions to trace (0.0 to 1.0)
# Default: 0.1 (10% of requests)
SENTRY_TRACES_SAMPLE_RATE=0.1

# Percentage of transactions to profile (0.0 to 1.0)
# Default: 0.1 (10% of requests)
SENTRY_PROFILES_SAMPLE_RATE=0.1
